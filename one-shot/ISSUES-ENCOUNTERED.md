# Issues Encountered

This log was maintained during the build. Failed attempts and superseded conclusions remain recorded. The generated fallback is always called a **candidate**, never a canonical SFHS artifact.

| Issue ID | Phase discovered | Status | Severity | Observed behavior | Expected behavior | Evidence | Suspected cause | Attempts made | Resolution or workaround | Affected files | Verification performed | Remaining risk |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| ENV-001 | Repository preflight | BLOCKED | Critical | No local SFHS checkout was found and shell `git clone` could not resolve `github.com`. | Pinned repository available locally before canonical authoring commands. | `evidence/preflight-command-log.txt`; connector-backed reads of repository commit and files. | Execution-network policy. | Filesystem search, clone, raw URL, Chromium navigation, GitHub connector. | Authored source against the pinned `main` commit and retrieved real `AGENTS.md`, plugin, minimal example, adapter, runtime, and manifest contracts. | Canonical CLI, packer, verifier, repository browser runner. | Working-tree state and canonical commands remain unavailable. |
| ENV-002 | Runtime preflight | SUPERSEDED | Major | Initial shell check found Node v22.16.0 and led to the conclusion that Node 24 was unavailable. | Node >=24 matching the pinned repository. | Original preflight log; `evidence/node24-source-tests.log`. | Node 24 was embedded in Playwright’s driver rather than on `PATH`. | Broader runtime search. | Found Node v24.11.1 at `/opt/pyvenv/lib/python3.13/site-packages/playwright/driver/node`; reran all focused source tests with it. | Source tests. | Does not provide pnpm or an installed SFHS workspace. |
| ENV-003 | Dependency acquisition | WORKAROUND | Major | The installed Pixi/SFHS workspace could not be acquired. Uploaded `pixijs-dev.zip` was upstream Pixi source, not a built package or SFHS repository. | Pinned workspace dependencies installed by pnpm. | `evidence/pixi-custom-build.json`; `evidence/pixi-smoke-webgl.json`. | Registry/repository access blocked. | Clone, registry, CDN, GitHub archive, connected repository reads, uploaded source inspection. | Built a candidate-only Pixi 8.19.0 browser bundle from the uploaded upstream source. It is not used to claim canonical pack or adapter verification. | `candidate/index.unverified.html`; candidate build tools outside source ZIP. | Candidate uses bounded local shims for Pixi’s external source dependencies and therefore still requires canonical intake. |
| ENV-004 | Canonical package manager | BLOCKED | Critical | pnpm 11.9.0 and the installed workspace are absent; Corepack/package acquisition cannot complete. | `pnpm sfhs ...` available from pinned repository root. | Preflight command log. | Package gateway/network restriction. | `pnpm`, Corepack, npm/registry, filesystem search. | None; no substitute CLI, packer, scanner, descriptor, or verifier was created. | Inspect, validate, check, pack, verify. | Canonical `dist/index.html` cannot be produced here. |
| CTRL-001 | Input lifecycle hardening | RESOLVED | Major | Surface listener cleanup closures referenced the mutable current surface and could remove from the wrong element after rebinding. | Cleanup removes listeners from the exact previously bound surface. | Source diff; input lifecycle evidence. | Closure captured mutable `surface`. | Split surface/global removal ownership; capture local `boundSurface`. | `src/input.ts` | 11/11 input lifecycle tests pass under Node 24. | Physical pointer-capture behavior remains pending. |
| MATCH-001 | Complete match-loop testing | RESOLVED | Major | First match-flow run lost ready/countdown/win events when several fixed steps occurred before presentation. | Every emitted event remains available exactly once to presentation/audio. | Preserved `evidence/match-flow-failed-attempt.json`; passing match-flow evidence. | `state.events` only represented the newest fixed step. | Added bounded 180-tick immutable event history; consumers deduplicate by monotonic event ID. | `src/physics.ts`, `src/presentation.ts`, `src/audio.ts`, `tests/match-flow.ts` | 6/6 match-flow and 16/16 physics scenarios pass. Browser candidate also observed paw-hit and goal events. | None known at source level. |
| AUDIO-001 | Audio polish | RESOLVED | Moderate | Every pointer acquisition could replay the audio-unlock confirmation. | Audio resumes on intentional gesture, but confirmation sounds once. | Source review; browser candidate audio status `ready`. | Missing one-time guard. | Added `unlockConfirmed`. | `src/audio.ts` | Trusted Chromium click unlocked AudioContext; browser status became `ready`. | Physical phone latency and mix balance remain UNVERIFIED. |
| CAND-001 | Candidate Pixi build | RESOLVED | Major | First custom Pixi source bundle threw while assigning `.default` to a primitive shader string. | Upstream shader modules load and WebGL renderer initializes. | Failed `evidence/pixi-smoke.json`; passing `evidence/pixi-smoke-webgl.json`. | Raw shader module wrapper used an invalid CommonJS default assignment. | Changed raw shader/JSON wrappers to export values without mutating primitives; rebuilt. | Candidate-only Pixi bundle. | WebGL2 `WebGLRenderer` smoke passed with one visible canvas. | Candidate bundle is still outside canonical SFHS pack. |
| BROWSER-001 | Browser proof | WORKAROUND | Critical | Real SFHS browser runner could not run without repository/workspace. | Canonical browser scenarios against exact `dist/index.html`. | `evidence/candidate-browser-scenarios.json`. | ENV-001 and ENV-004. | Used installed Playwright and Chromium against the exact self-contained candidate, without claiming canonical runner equivalence. | Candidate evidence. | Clean boot, Pixi/WebGL2, one canvas, audio unlock, input ownership, one goal, pause, resize, rotate gate, zero requests, and zero errors passed. | Canonical artifact/browser runner remains blocked. |
| BROWSER-002 | Browser loading | WORKAROUND | Major | Chromium URL navigation to local files, localhost, GitHub, and npm returned `ERR_BLOCKED_BY_ADMINISTRATOR`. | Load candidate normally through file or HTTP URL. | Navigation probe; candidate browser evidence. | Browser policy in execution image. | `file://`, localhost HTTP, external HTTPS, headless/headful. | Loaded exact HTML bytes through Playwright `page.set_content`; no runtime external requests were needed. | Browser evidence method. | File-protocol smoke and ordinary URL navigation remain UNTESTED. |
| BROWSER-003 | WebGL availability | SUPERSEDED | Major | Headless Chromium returned no WebGL context and Pixi selected its upstream Canvas fallback during a standalone smoke attempt. | WebGL-required path only. | Failed `evidence/pixi-smoke.json`; passing WebGL smoke. | Headless GPU policy. | Multiple GL/ANGLE flags; headful Chromium under Xvfb. | Headful Xvfb exposed WebGL2; game’s required capability gate prevents Canvas fallback when WebGL is absent. | Browser environment. | Software WebGL is screening evidence, not physical-GPU acceptance. |
| EVID-001 | Full browser match automation | UNVERIFIED | Moderate | An attempted autonomous first-to-five browser run timed out after an inadequate control strategy; it did not expose a deterministic game failure. | Browser automation reaches win and rematch. | Preserved tool attempt; final bounded browser evidence; Node 24 match-flow evidence. | Automated player failed to retrieve/aim a stopped puck reliably. | Replaced with browser proof of a real ready/countdown/paw-hit/goal path and source-level exact win/rematch scenarios. | Browser evidence only. | Full browser first-to-five and rematch remain UNTESTED; source simulation passes both. |
| VIS-001 | Graphics and orientation | WORKAROUND | Major | Initial report had no rendered evidence. | Phone-sized screenshots proving visual hierarchy and orientation gate. | `evidence/browser-01-ready.png` through `browser-04-landscape-gate.png`. | Canonical dependencies initially unavailable. | Candidate Pixi source bundle and Chromium WebGL proof. | Procedural presentation and CSS. | Ready, play, goal, and rotate-gate screenshots captured; no browser errors. | Finger occlusion, exact phone density, and physical readability remain UNVERIFIED. |
| VERIFY-001 | Canonical pack and exact verify | BLOCKED | Critical | `pnpm sfhs pack` and `pnpm sfhs verify` cannot run. | Canonical `dist/index.html` with descriptor, source binding, exact bytes/hash, and external-reference findings. | Preflight logs; absence of `dist/`. | Missing checkout, pnpm, and installed workspace. | Preserved source, created only `candidate/index.unverified.html`, maintained required labels. | Deliverables. | Candidate hash/static/browser evidence exists; canonical commands remain unrun. | Requested canonical `cat-air-hockey-index.html` does not exist. |
| MOBILE-001 | Physical acceptance | UNVERIFIED | Major | Exact packed artifact was not tested on Samsung Galaxy S21 Ultra. | Physical two-finger, browser chrome, comfort, occlusion, sound, and performance acceptance. | Physical checklist in report. | No physical-device access and no canonical artifact. | Mobile-sized Chromium viewport and synthetic two-pointer screening. | None can replace physical testing. | 540×960 candidate screening passed. | All physical acceptance remains pending. |

## Candidate-only build note

The candidate exists to make the work playable despite the canonical blocker. It is explicitly labeled:

```text
CANDIDATE_BUILT
SFHS_PACK_NOT_RUN
SFHS_VERIFY_NOT_RUN
INTAKE_REQUIRED
```

It was not written to `dist/`, and it must not be renamed to the canonical artifact until the real SFHS pack and exact verify succeed.

## Graduation continuation

| Issue ID | Status | Resolution / current truth |
|---|---|---|
| OSF-GRAD-003 | INVESTIGATING | The legacy `sfhs.project@1` manifest declared `viewport.orientation: "portrait"`; current SFHS requires `"adaptive"`. The authoritative source retains its portrait-first design through a pause-and-guide landscape policy while adopting the valid contract. Canonical verification will decide resolution. |
| ENV-001 | SUPERSEDED | A pinned SFHS checkout and real materializer are now available for graduation. |
| ENV-004 | SUPERSEDED | The canonical path is available through the pinned SFHS materialization; the historical candidate remains noncanonical. |
| VERIFY-001 | SUPERSEDED | This historical Chat blocker is replaced by the current real SFHS pack and exact-verifier gate. |
