# CATPAW-EXPANSION-001-R2 execution plan

Status: **PROPOSED** until the exact packed artifact passes the pinned SFHS gates.

## Authority and bounded change

Readable `src/`, tests, project records, and the current 540×960 Pixi layout are authoritative. R2 changes presentation and local Board artwork only: one shared center-edge control set, one replaceable rectangular Board texture, exact template export, strict local loading, persistence, and directly coupled proof. Physics, legal halves, adjustable goals, return-speed behavior, actors, match flow, and the single SFHS renderer remain unchanged.

## Implementation sequence

1. Reconcile `main` into `codex/fullscreen-menu-theme-001` without discarding either lineage.
2. Replace duplicated end controls with Mute upper-left, Pause/Capture lower-left, Menu upper-right, and Fullscreen lower-right at the center side edges.
3. Separate static Board presentation from dynamic goals/cats/actors and install one persistent texture-backed Board sprite with procedural fallback.
4. Derive the template from the full 540×960 logical Board at the existing DPR-2 ceiling, validate exact 1080×1920 PNG replacements, and persist the accepted Board independently.
5. Add focused control/Board/layout tests, packed Chromium evidence, completion records, and an artifact-bound Samsung seed.
6. Run source tests, fresh pinned materialization lint/typecheck/test, One-Shot inspect/audit, SFHS inspect/validate/check/pack/verify, and packed browser evidence; then create one local commit.

## Rollback

The pre-R2 source is retained in Git at merge commit `9233b154fa3e4a92fbca03758db7d9a3d52656c6`. Generated `dist/` and test evidence remain ignored. No remote mutation is authorized.
